import { Auditable } from "./auditable";

export class Acc_TotalSpacesRange extends Auditable {
    totalSpacesRangeId: number;
    totalSpacesRengeName: string | null;
    rangeValue: number | null;
}